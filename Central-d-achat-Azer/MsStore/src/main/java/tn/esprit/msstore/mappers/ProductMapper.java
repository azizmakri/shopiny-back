package tn.esprit.msstore.mappers;

import tn.esprit.msstore.Entity.Product;
import tn.esprit.msstore.dtoEntities.ProductDto;

public class ProductMapper {
   /* public static ProductDto mapProductToDto( Product product) {

        ProductDto productDto = ProductDto.builder()
                .idProduct(product.getIdProduct())
                .descriptionProduct(product.getDescriptionProduct())
                .nameProduct(product.getNameProduct())
                .priceProduct(product.getPriceProduct())
                .discountProduct(product.getDiscountProduct())
                .imageProduct(product.getImageProduct())
                .quantityProduct(product.getQuantityProduct())
                .referenceProduct(product.getReferenceProduct())
                .marqueProduct(product.getMarqueProduct())
                .dateCreationProduct(product.getDateCreationProduct())

                .build();
        return productDto;
    }
    public static Product mapProductToEntity(ProductDto productDto){

        Product product = Product.builder()
                .idProduct(productDto.getIdProduct())
                .descriptionProduct(productDto.getDescriptionProduct())
                .marqueProduct(productDto.getMarqueProduct())
                .discountProduct(productDto.getDiscountProduct())
                .nameProduct(productDto.getNameProduct())
                .quantityProduct(productDto.getQuantityProduct())
                .priceProduct(productDto.getPriceProduct())
                .referenceProduct(productDto.getReferenceProduct())
                .imageProduct(productDto.getImageProduct())
                .dateCreationProduct(productDto.getDateCreationProduct())
                .build();
        return product;
    }*/

}
